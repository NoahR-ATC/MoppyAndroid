<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/slide_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/song_title_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/song_titlebar_height"
        android:background="#50AC50">

        <TextView
            android:id="@+id/song_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|center_vertical"
            android:layout_marginHorizontal="?attr/dialogPreferredPadding"
            android:layout_marginVertical="?attr/listPreferredItemPaddingStart"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:singleLine="true"
            android:text="@string/song_title" />
    </FrameLayout>

    <ImageView
        android:id="@+id/image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/sequencer_fragment_txt"
        app:srcCompat="@color/colorPrimaryDark" />

    <SeekBar
        android:id="@+id/seekBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/toolbar_layout_placeholder"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="10dp" />

    <FrameLayout
        android:id="@+id/toolbar_layout_placeholder"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_button_height"
        android:layout_alignParentBottom="true">

    </FrameLayout>

</RelativeLayout>